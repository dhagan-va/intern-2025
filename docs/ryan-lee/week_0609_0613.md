## 6/9

- Looked over Agile Course in Percipio
- Researched where to store sponsor IDs
    - Currently reading documentation/pricing on MongoDB
- Daily Scrum
- Chatted with Justin/Code Review/Clarified requirements

### Tasks finished

- Refactored some var names to be legible
- Randomize AMT so that it shows accurate values ($ and visits)
- Implemented initialization of MongoDB databases

### Next Steps

- Add way to store Sponsor IDs and their beneficiarys (personal info)
- Check to make sure all transactions contain a unique SSN and unique beneficiary ID
- Implement a binomial distribution for the tests (so that it follows the standard production rate) \[Needs fine tuning\]
- Adjustable Error rate
- Change variable names to business names for readability

## 6/10

- Refactored code structure
- Created Data type for Sponsors/Beneficiaries
- Ensured the generation of unique SSNs and sponsor/beneficiary IDs

### Tasks finished

- Refactored code structure
    - Breakdown of separation of files:
        - Segment generation (to segment out EDI file)
        - Data generation 
        - File generation
        - Data storage (to be added)
    - Added the generation of unique SSNs and sponsor/beneficiary IDs

### Next Steps

- Add data to MongoDB
    - Research mongoDB/indexing for fast querying
- Implement a binomial distribution for the tests (so that it follows the standard production rate) \[Needs fine tuning\]
- Adjustable Error rate
- Change variable names to business names for readability

## 6/11

- Determined slowdown of Data Generation
- Meeting to discuss database implementation really helped a lot
    - Have 3 layers:
    - Data: generic getX (getbyID, getSsn, etc)
    - Repository: calls to database (if you ever need to change DB, change method/file in repository)
    - UI: UI 👍

### Tasks finished

- Corrected message generation for 834 file
- Altered the creation of sponsor/beneficiary IDs to use SSNs

### Next Steps

- Make abstract class/interface for DB functions
- Add data to MongoDB/other DB
    - Research mongoDB/indexing for fast querying
- Implement a binomial distribution for the tests (so that it follows the standard production rate) \[Needs fine tuning\]
- Adjustable Error rate
- Change variable names to business names for readability
- Take time to learn indexing, partitioning for querying

## 6/12

- Created the interface for the database functions
- Created JSON local library
- Debugged randomization of names/SSNs
- Debugged data structure to JSON and JSON to data structure

### Tasks finished

- Corrected message generation for 834 file
- Restructured files for database access/database functions
- Completed creation of local database
    - Creation of data might be O(n<sup>2.x</sup>)
    - It takes 40 seconds to create 10,000 messages and 330 seconds to create 20,000 messages

### Next Steps

- Need to add MongoDB functions
- Add data to MongoDB/other DB
    - Research mongoDB/indexing for fast querying
- Implement a binomial distribution for the tests (so that it follows the standard production rate) \[Needs fine tuning\]
- Adjustable Error rate
- Change variable names to business names for readability
- Take time to learn indexing, partitioning for querying