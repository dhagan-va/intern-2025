## 6/16

- Finished Agile course on percipio
- Unit test for checking if there are same SSNs
- Added user limit to local database
- Created adjustable error rate on messages of 0.5% or 5 messages every 1000 messages created

### Tasks finished

- Unit test for checking if there are same SSNs
- User Limit for local DB
- Adjustable error rate on messages

### Next Steps

- Logging in new files
- Adjustable error messages/rates
- Fine tune binom distribution for messages
- Change variable names to business names for readability

- Learn indexing, partitioning for querying
- Format JSONL to JSON
    - Upload JSON to Mongo
- Add data to MongoDB/other DB
    - Research mongoDB/indexing for fast querying

## 6/17

- Code review with Justin to go over PR 
- Made adjustments to code to follow the feedback given
- Refactored files so there is less hard coding
- Output is all stored in one folder now

### Tasks finished

- Logging in previous files
- Unit test to create 1 message

### Next Steps

- Create unit tests for erroneous message contents and ensure they match the number
- Fine tune binom distribution for messages
- Change variable names to business names for readability

- Learn indexing, partitioning for querying
- Format JSONL to JSON
    - Upload JSON to Mongo
- Add data to MongoDB/other DB
    - Research mongoDB/indexing for fast querying
    
## 6/18

- Made message contents uppercase
- Reviewed code with Justin
- Unit tests for edi validity
- Refactored by removing sponsors from in 834 messages

### Tasks finished

- Made message contents uppercase
- Extensive logging
- Refactored code

### Next Steps

- See if any methods could be condensed
- Delete output directory after test is run
- Make each test run with a newly generated edi file
- Fine tune binom distribution for messages
- Change variable names to business names for readability

- Learn indexing, partitioning for querying
- Format JSONL to JSON
    - Upload JSON to Mongo
- Add data to MongoDB/other DB
    - Research mongoDB/indexing for fast querying

## 6/19

Juneteenth

## 6/20

- Finished Unit Tests
- Refactored data generation so storage and creation is 2 separate functions
- Started working on 270 forms

### Tasks finished

- Each unit test creates its own EDI file and removes the output directory upon completing
- Split up sponsor/beneficiary data creation and storage
- Error rates test now properly calculated

### Next Steps

- See if any methods could be condensed
- Delete output directory after test is run
- Make each test run with a newly generated edi file
- Fine tune binom distribution for messages
- Change variable names to business names for readability

- Learn indexing, partitioning for querying
- Format JSONL to JSON
    - Upload JSON to Mongo
- Add data to MongoDB/other DB
    - Research mongoDB/indexing for fast querying